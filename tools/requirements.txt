# Python deps for the SCINTIX P4 DOOM agent tools (needs Python 3.10+).
#   pip install -r tools/requirements.txt
websockets>=12.0   # agent_client.py and the MCP bridge
mcp>=1.2.0         # doom_mcp_server.py (Model Context Protocol server)
